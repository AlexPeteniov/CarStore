export class Property {
  public name: string;
  public type: string;
  public id: string;

  constructor(name: string, type: string, id: string) {
    this.name = name;
    this.type = type;
    this.id = id;
  }
}
